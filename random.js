var num = 4.4874;
var folafol = Math.floor(num);
var folafol2 = Math.ceil(num);
var folafol3 = Math.round(num);
for( var i = 0; i < 7; i++){
    var randomNum = Math.random() * 7;
    var output = Math.round(randomNum);
    console.log(output);
}
